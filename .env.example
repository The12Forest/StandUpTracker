# MongoDB connection string (only required environment variable)
MONGO_URI=mongodb://localhost:27017/standuptracker

# All other settings (JWT, SMTP, app config) are managed
# through the Admin Console and stored in the database.
# On first launch, the onboarding wizard will guide you through setup.
